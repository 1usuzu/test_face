--extra-index-url https://download.pytorch.org/whl/cpu

# DeepTrust Core API (backend)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Crypto / signing
eth-account>=0.13.0

# Blockchain client
web3>=6.17.0

# HTTP client
httpx>=0.27.0

# AI runtime (CPU-friendly wheels on Linux). If you deploy with CUDA, pin CUDA wheels explicitly.
torch==2.2.2+cpu
torchvision==0.17.2+cpu
torchaudio==2.2.2+cpu

# Imaging / numerics
numpy==1.26.4
pillow==10.2.0
opencv-python==4.11.0.86

# Optional face extractor (used if installed)
facenet-pytorch==2.6.0

# Config management (used by ai_deepfake/ai_config.py)
pydantic-settings>=2.0.0

# Testing
pytest>=8.0.0

